-- Code generated by sqd-go codegen; DO NOT EDIT.

CREATE DATABASE IF NOT EXISTS `polymarket`;

CREATE TABLE IF NOT EXISTS `polymarket`.sync_state (
  chain_id UInt64,
  last_block UInt64,
  last_hash String,
  finalized_block UInt64,
  finalized_hash String,
  rollback_chain String,
  updated_at DateTime64(3, 'UTC') DEFAULT now64(3)
) ENGINE = MergeTree()
ORDER BY (chain_id, updated_at);

CREATE TABLE IF NOT EXISTS `polymarket`.`conditional_tokens_condition_preparation_events` (
  block_number UInt64,
  block_timestamp DateTime64(3, 'UTC'),
  transaction_index UInt64,
  log_index UInt64,
  `conditionId` FixedString(32),
  `oracle` FixedString(20),
  `questionId` FixedString(32),
  `outcomeSlotCount` UInt256,
  sign Int8 DEFAULT 1
) ENGINE = CollapsingMergeTree(sign)
PRIMARY KEY (`conditionId`, `oracle`, block_number, transaction_index, log_index)
ORDER BY (`conditionId`, `oracle`, block_number, transaction_index, log_index);

CREATE TABLE IF NOT EXISTS `polymarket`.`conditional_tokens_condition_resolution_events` (
  block_number UInt64,
  block_timestamp DateTime64(3, 'UTC'),
  transaction_index UInt64,
  log_index UInt64,
  `conditionId` FixedString(32),
  `oracle` FixedString(20),
  `questionId` FixedString(32),
  `payoutDenominator` UInt256,
  `payoutNumerators` String,
  sign Int8 DEFAULT 1
) ENGINE = CollapsingMergeTree(sign)
PRIMARY KEY (`conditionId`, `oracle`, block_number, transaction_index, log_index)
ORDER BY (`conditionId`, `oracle`, block_number, transaction_index, log_index);

CREATE TABLE IF NOT EXISTS `polymarket`.`conditional_tokens_position_split_events` (
  block_number UInt64,
  block_timestamp DateTime64(3, 'UTC'),
  transaction_index UInt64,
  log_index UInt64,
  `stakeholder` FixedString(20),
  `collateralToken` FixedString(20),
  `parentCollectionId` FixedString(32),
  `conditionId` FixedString(32),
  `partition` String,
  `amount` UInt256,
  sign Int8 DEFAULT 1
) ENGINE = CollapsingMergeTree(sign)
PRIMARY KEY (`stakeholder`, `collateralToken`, block_number, transaction_index, log_index)
ORDER BY (`stakeholder`, `collateralToken`, block_number, transaction_index, log_index);

CREATE TABLE IF NOT EXISTS `polymarket`.`conditional_tokens_positions_merge_events` (
  block_number UInt64,
  block_timestamp DateTime64(3, 'UTC'),
  transaction_index UInt64,
  log_index UInt64,
  `stakeholder` FixedString(20),
  `collateralToken` FixedString(20),
  `parentCollectionId` FixedString(32),
  `conditionId` FixedString(32),
  `partition` String,
  `amount` UInt256,
  sign Int8 DEFAULT 1
) ENGINE = CollapsingMergeTree(sign)
PRIMARY KEY (`stakeholder`, `collateralToken`, block_number, transaction_index, log_index)
ORDER BY (`stakeholder`, `collateralToken`, block_number, transaction_index, log_index);

CREATE TABLE IF NOT EXISTS `polymarket`.`conditional_tokens_payout_redemption_events` (
  block_number UInt64,
  block_timestamp DateTime64(3, 'UTC'),
  transaction_index UInt64,
  log_index UInt64,
  `redeemer` FixedString(20),
  `collateralToken` FixedString(20),
  `parentCollectionId` FixedString(32),
  `conditionId` FixedString(32),
  `indexSets` String,
  `payout` UInt256,
  sign Int8 DEFAULT 1
) ENGINE = CollapsingMergeTree(sign)
PRIMARY KEY (`redeemer`, `collateralToken`, block_number, transaction_index, log_index)
ORDER BY (`redeemer`, `collateralToken`, block_number, transaction_index, log_index);

CREATE TABLE IF NOT EXISTS `polymarket`.`exchange_order_filled_events` (
  block_number UInt64,
  block_timestamp DateTime64(3, 'UTC'),
  transaction_index UInt64,
  log_index UInt64,
  `maker` FixedString(20),
  `taker` FixedString(20),
  `makerAssetId` UInt256,
  `takerAssetId` UInt256,
  `makerAmountFilled` UInt256,
  `takerAmountFilled` UInt256,
  `fee` UInt256,
  sign Int8 DEFAULT 1
) ENGINE = CollapsingMergeTree(sign)
PRIMARY KEY (`maker`, `taker`, block_number, transaction_index, log_index)
ORDER BY (`maker`, `taker`, block_number, transaction_index, log_index);

CREATE TABLE IF NOT EXISTS `polymarket`.`neg_risk_exchange_order_filled_events` (
  block_number UInt64,
  block_timestamp DateTime64(3, 'UTC'),
  transaction_index UInt64,
  log_index UInt64,
  `maker` FixedString(20),
  `taker` FixedString(20),
  `makerAssetId` UInt256,
  `takerAssetId` UInt256,
  `makerAmountFilled` UInt256,
  `takerAmountFilled` UInt256,
  `fee` UInt256,
  sign Int8 DEFAULT 1
) ENGINE = CollapsingMergeTree(sign)
PRIMARY KEY (`maker`, `taker`, block_number, transaction_index, log_index)
ORDER BY (`maker`, `taker`, block_number, transaction_index, log_index);

CREATE TABLE IF NOT EXISTS `polymarket`.`neg_risk_adapter_market_prepared_events` (
  block_number UInt64,
  block_timestamp DateTime64(3, 'UTC'),
  transaction_index UInt64,
  log_index UInt64,
  `marketId` FixedString(32),
  `creator` FixedString(20),
  `feeBips` UInt256,
  `data` String,
  sign Int8 DEFAULT 1
) ENGINE = CollapsingMergeTree(sign)
PRIMARY KEY (`marketId`, `creator`, block_number, transaction_index, log_index)
ORDER BY (`marketId`, `creator`, block_number, transaction_index, log_index);

CREATE TABLE IF NOT EXISTS `polymarket`.`neg_risk_adapter_question_prepared_events` (
  block_number UInt64,
  block_timestamp DateTime64(3, 'UTC'),
  transaction_index UInt64,
  log_index UInt64,
  `marketId` FixedString(32),
  `questionId` FixedString(32),
  `index` UInt256,
  `data` String,
  sign Int8 DEFAULT 1
) ENGINE = CollapsingMergeTree(sign)
PRIMARY KEY (`marketId`, `questionId`, block_number, transaction_index, log_index)
ORDER BY (`marketId`, `questionId`, block_number, transaction_index, log_index);

CREATE TABLE IF NOT EXISTS `polymarket`.`neg_risk_adapter_position_split_events` (
  block_number UInt64,
  block_timestamp DateTime64(3, 'UTC'),
  transaction_index UInt64,
  log_index UInt64,
  `stakeholder` FixedString(20),
  `conditionId` FixedString(32),
  `amount` UInt256,
  sign Int8 DEFAULT 1
) ENGINE = CollapsingMergeTree(sign)
PRIMARY KEY (`stakeholder`, `conditionId`, block_number, transaction_index, log_index)
ORDER BY (`stakeholder`, `conditionId`, block_number, transaction_index, log_index);

CREATE TABLE IF NOT EXISTS `polymarket`.`neg_risk_adapter_positions_merge_events` (
  block_number UInt64,
  block_timestamp DateTime64(3, 'UTC'),
  transaction_index UInt64,
  log_index UInt64,
  `stakeholder` FixedString(20),
  `conditionId` FixedString(32),
  `amount` UInt256,
  sign Int8 DEFAULT 1
) ENGINE = CollapsingMergeTree(sign)
PRIMARY KEY (`stakeholder`, `conditionId`, block_number, transaction_index, log_index)
ORDER BY (`stakeholder`, `conditionId`, block_number, transaction_index, log_index);

CREATE TABLE IF NOT EXISTS `polymarket`.`neg_risk_adapter_positions_converted_events` (
  block_number UInt64,
  block_timestamp DateTime64(3, 'UTC'),
  transaction_index UInt64,
  log_index UInt64,
  `stakeholder` FixedString(20),
  `marketId` FixedString(32),
  `indexSet` UInt256,
  `amount` UInt256,
  sign Int8 DEFAULT 1
) ENGINE = CollapsingMergeTree(sign)
PRIMARY KEY (`stakeholder`, `marketId`, block_number, transaction_index, log_index)
ORDER BY (`stakeholder`, `marketId`, block_number, transaction_index, log_index);

CREATE TABLE IF NOT EXISTS `polymarket`.`neg_risk_adapter_payout_redemption_events` (
  block_number UInt64,
  block_timestamp DateTime64(3, 'UTC'),
  transaction_index UInt64,
  log_index UInt64,
  `redeemer` FixedString(20),
  `conditionId` FixedString(32),
  `amounts` String,
  `payout` UInt256,
  sign Int8 DEFAULT 1
) ENGINE = CollapsingMergeTree(sign)
PRIMARY KEY (`redeemer`, `conditionId`, block_number, transaction_index, log_index)
ORDER BY (`redeemer`, `conditionId`, block_number, transaction_index, log_index);

CREATE TABLE IF NOT EXISTS `polymarket`.`fixed_product_market_maker_factory_fixed_product_market_maker_creation_events` (
  block_number UInt64,
  block_timestamp DateTime64(3, 'UTC'),
  transaction_index UInt64,
  log_index UInt64,
  `creator` FixedString(20),
  `fixedProductMarketMaker` FixedString(20),
  `conditionalTokens` FixedString(20),
  `collateralToken` FixedString(20),
  `conditionIds` String,
  `fee` UInt256,
  sign Int8 DEFAULT 1
) ENGINE = CollapsingMergeTree(sign)
PRIMARY KEY (`creator`, `fixedProductMarketMaker`, block_number, transaction_index, log_index)
ORDER BY (`creator`, `fixedProductMarketMaker`, block_number, transaction_index, log_index);

CREATE TABLE IF NOT EXISTS `polymarket`.`fixed_product_market_maker_fpmmbuy_events` (
  block_number UInt64,
  block_timestamp DateTime64(3, 'UTC'),
  transaction_index UInt64,
  log_index UInt64,
  `buyer` FixedString(20),
  `investmentAmount` UInt256,
  `feeAmount` UInt256,
  `outcomeIndex` UInt256,
  `outcomeTokensBought` UInt256,
  sign Int8 DEFAULT 1
) ENGINE = CollapsingMergeTree(sign)
PRIMARY KEY (`buyer`, `investmentAmount`, block_number, transaction_index, log_index)
ORDER BY (`buyer`, `investmentAmount`, block_number, transaction_index, log_index);

CREATE TABLE IF NOT EXISTS `polymarket`.`fixed_product_market_maker_fpmmsell_events` (
  block_number UInt64,
  block_timestamp DateTime64(3, 'UTC'),
  transaction_index UInt64,
  log_index UInt64,
  `seller` FixedString(20),
  `returnAmount` UInt256,
  `feeAmount` UInt256,
  `outcomeIndex` UInt256,
  `outcomeTokensSold` UInt256,
  sign Int8 DEFAULT 1
) ENGINE = CollapsingMergeTree(sign)
PRIMARY KEY (`seller`, `returnAmount`, block_number, transaction_index, log_index)
ORDER BY (`seller`, `returnAmount`, block_number, transaction_index, log_index);

CREATE TABLE IF NOT EXISTS `polymarket`.`fixed_product_market_maker_fpmmfunding_added_events` (
  block_number UInt64,
  block_timestamp DateTime64(3, 'UTC'),
  transaction_index UInt64,
  log_index UInt64,
  `funder` FixedString(20),
  `amountsAdded` String,
  `sharesMinted` UInt256,
  sign Int8 DEFAULT 1
) ENGINE = CollapsingMergeTree(sign)
PRIMARY KEY (`funder`, `amountsAdded`, block_number, transaction_index, log_index)
ORDER BY (`funder`, `amountsAdded`, block_number, transaction_index, log_index);

CREATE TABLE IF NOT EXISTS `polymarket`.`fixed_product_market_maker_fpmmfunding_removed_events` (
  block_number UInt64,
  block_timestamp DateTime64(3, 'UTC'),
  transaction_index UInt64,
  log_index UInt64,
  `funder` FixedString(20),
  `amountsRemoved` String,
  `collateralRemovedFromFeePool` UInt256,
  `sharesBurnt` UInt256,
  sign Int8 DEFAULT 1
) ENGINE = CollapsingMergeTree(sign)
PRIMARY KEY (`funder`, `amountsRemoved`, block_number, transaction_index, log_index)
ORDER BY (`funder`, `amountsRemoved`, block_number, transaction_index, log_index);
